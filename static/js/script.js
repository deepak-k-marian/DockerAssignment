document.addEventListener('DOMContentLoaded', () => {
    const btn = document.getElementById('main-btn');
    
    btn.addEventListener('click', () => {
        alert("You clicked the Hello World button! The frontend is completely wired up.");
    });
});
